# k

"Safe" attribute getter operators for python. Useful when exploring data in a REPL for debugging.

## Usage

	import k
	name = k.name(user)
	names = k.name(users)
	message_bodies = k.messages.body(users)

